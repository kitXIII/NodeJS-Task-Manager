extends ../layouts/application.pug

block content
  +formFor(f, { url: urlFor('session'), method: 'post' })
    +input(f, 'email')
    +input(f, 'password', { as: 'password' })
    +button(f, { value: 'Sign In', classList: 'btn-info' })
